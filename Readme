# Docker instructions

Start a docker data volume by

    docker run -v /data -name DATA tianon/true

Start the docker container for sidekiq workers

    docker run --volumes-from DATA bibstha/bharkhar-workers


